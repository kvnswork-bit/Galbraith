rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /site_content/{contentId} {
      // Allow public read and write access for site content
      allow read: if true;
      allow write: if true;
    }
  }
}
